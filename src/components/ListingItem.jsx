import React from "react";
import { Col } from "react-bootstrap";

const ListingItem = ({
  id,
  title,
  price,
  description,
  location,
  img,
  isLarge,
  isCollection,
  count,
}) => {
  return (
    <Col>
      
    </Col>
  );
};

export default ListingItem;
